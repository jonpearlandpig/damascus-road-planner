# Visual QA - 2026-07-18

Branch: `codex/source-reconciliation-visual-qa`

Local server: `npm run dev -- --host 127.0.0.1`

Browser tool: `/browse`

## Routes And Widths

Routes checked:

- `/`
- `/compare`
- `/venues/spectrum-center`
- Targeted conflict check: `/venues/van-andel-arena`

Widths checked:

- Desktop: 1440 x 900
- Laptop: 1280 x 820
- Tablet: 768 x 1024
- Mobile: 390 x 844

## Results

- Dashboard: no document-level horizontal overflow at all checked widths. Mobile cards stack cleanly and source availability labels are visible.
- Comparison: no document-level horizontal overflow. The table uses internal horizontal scroll below full desktop width. Measurement status and confidence labels remain readable.
- Spectrum workspace: lazy loading state appears while the scene chunk loads, then settles to a nonblank 3D canvas. Desktop, tablet, and mobile canvas dimensions are stable.
- Mobile workspace: fixed default source drawer behavior so the 3D scene is visible on first load. The drawer still opens after object selection.
- Disabled controls: Saved views and Measure controls are disabled and visible on desktop and mobile. Mobile bottom bar now shows these controls first.
- Source/provenance labels: source drawer shows availability, source type, controlling/reference status, and conflict flags where present.
- Van Andel conflict state: unresolved rigging-height warning appears in the scene, source availability is labeled Missing, and conflict flags are visible in the drawer.
- Route transitions: dashboard to comparison and dashboard to Spectrum workspace work in browser smoke tests.
- Unknown venue route: `/venues/not-a-real-venue` falls back to the dashboard in browser smoke tests.

## Console

- Dashboard and comparison produced no console errors.
- Venue canvas routes produced no app exceptions.
- Remaining warnings during canvas capture:
  - `THREE.Clock` deprecation from the R3F/Three runtime.
  - WebGL `ReadPixels` performance warnings during screenshot capture.
- Removed the app-owned `PCFSoftShadowMap` warning by setting the canvas shadow type explicitly.

## Artifacts

Screenshots were captured locally under ignored `qa-artifacts/source-reconciliation-visual-qa/`.

Representative settled captures:

- `venue-spectrum-desktop-settled.png`
- `venue-spectrum-mobile-controls-fixed.png`
- `van-andel-conflict-desktop-fixed.png`
- `home-mobile.png`
- `compare-mobile.png`
